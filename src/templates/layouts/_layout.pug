include ../mixins/all
doctype html
html
  head
    include _head

  body.no-scroll(class=bodyClass)
    div#overlay
    div#preloader
    //BEGIN out
    .out(class=outClass)
      block header
        include ../partials/_header
      block content
    //END out

    //LOAD SCRIPTS
    block scripts
      script(type="text/javascript" src="js/app.js")
